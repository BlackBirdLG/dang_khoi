numbers = [42, 17, 89, 5, 23, 71]
max_value = numbers[0]
min_value = numbers[0]
for num in numbers:
    if num > max_value:
        max_value = num
    elif num < min_value:
        min_value = num

print(f"Maximum: {max_value}")
print(f"Minimum: {min_value}")